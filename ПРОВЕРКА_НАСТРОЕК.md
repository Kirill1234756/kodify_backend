# ✅ Контрольный список проверки настроек

## 📋 Проверка .env файла на VPS

### 1. Telegram Bot Token ✅

**Текущее значение в nano:**
```
TELEGRAM_BOT_TOKEN=8226522901:AAHJSZ8zpZ-UCSX40TmKb-bLb12FwTgzGyA
```

**✅ Проверка:**
- [x] Нет лишнего подчеркивания `_` в конце - **ИСПРАВЛЕНО**
- [ ] Формат правильный: `число:буквы_и_цифры`
- [ ] Нет пробелов в начале/конце
- [ ] Соответствует токену из BotFather

**Ожидаемый формат:** `8226522901:AAHJSZ8zpZ-UCSX40TmKb_bLb12FwTgzGyA`

**⚠️ ВАЖНО:** Сравните с токеном из BotFather. Если токен из BotFather:
```
8226522901:AAHJSZ8zpZ-UCSX40TmKb_bLb12FwTgzGyA
```

То в .env должно быть **точно так же**, включая все символы `-` и `_`.

---

### 2. Telegram Chat ID ✅

**Текущее значение:**
```
TELEGRAM_CHAT_ID=-1003136896464
```

**✅ Проверка:**
- [x] Для группы начинается с `-` - **ПРАВИЛЬНО**
- [ ] ID соответствует группе "Kodify application" в Telegram
- [ ] Бот добавлен в эту группу
- [ ] Бот может отправлять сообщения в группу

**Как проверить chat_id:**
1. Добавьте бота в группу (если еще не добавлен)
2. Отправьте сообщение в группу
3. Откройте: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
4. Найдите в ответе `"chat":{"id":-1003136896464}`

---

### 3. PUBLIC_URL ✅

**Текущее значение:**
```
PUBLIC_URL=https://api.kodifyweb.ru
```

**✅ Проверка:**
- [x] Использует `https://` (не `http://`) - **ИСПРАВЛЕНО**
- [x] Домен правильный: `api.kodifyweb.ru`
- [x] Нет слеша `/` в конце

**Правильно:** ✅ `https://api.kodifyweb.ru`  
**Неправильно:** ❌ `http://api.kodifyweb.ru`  
**Неправильно:** ❌ `https://api.kodifyweb.ru/`

---

### 4. Другие важные настройки

**Проверьте также:**

```env
NODE_ENV=production
PORT=3000

FRONTEND_URL=https://kodifyweb.ru
FRONTEND_URLS=https://kodifyweb.ru,http://kodifyweb.ru

DB_HOST=postgres
DB_PORT=5432
DB_NAME=kodify_db
DB_USER=kodify_user
DB_PASSWORD=ваш_пароль
```

---

## 🔍 Финальная проверка после сохранения

После того как вы сохраните файл (`Ctrl+O`, `Enter`, `Ctrl+X`), выполните:

### 1. Перезапустите бекенд:
```bash
docker-compose restart backend
```

### 2. Проверьте токен через API:
```bash
curl "https://api.telegram.org/bot8226522901:AAHJSZ8zpZ-UCSX40TmKb_bLb12FwTgzGyA/getMe"
```

Если токен правильный, вернется информация о боте:
```json
{
  "ok": true,
  "result": {
    "id": 8226522901,
    "is_bot": true,
    "first_name": "kodifytg_bot",
    ...
  }
}
```

Если токен неправильный, вернется:
```json
{
  "ok": false,
  "error_code": 401,
  "description": "Unauthorized"
}
```

### 3. Проверьте тестовый endpoint:
```bash
curl https://api.kodifyweb.ru/api/test/telegram
```

Должно вернуть:
```json
{
  "success": true,
  "message": "Telegram bot is working"
}
```

### 4. Проверьте логи:
```bash
docker-compose logs --tail=50 backend | grep -i telegram
```

Ищите:
- ✅ `Telegram bot configuration test successful` - успех
- ❌ `ETELEGRAM: 401 Unauthorized` - все еще ошибка

---

## ✅ Итоговый чеклист:

- [ ] `TELEGRAM_BOT_TOKEN` скопирован точно из BotFather (без лишних символов)
- [ ] `TELEGRAM_CHAT_ID` соответствует группе
- [ ] `PUBLIC_URL` использует `https://`
- [ ] Файл сохранен (`Ctrl+O`, `Enter`, `Ctrl+X`)
- [ ] Бекенд перезапущен
- [ ] Тестовый endpoint возвращает `success: true`
- [ ] В логах нет ошибок `401 Unauthorized`

---

## 🎯 Если все проверки пройдены:

1. Отправьте тестовую форму на сайте
2. Проверьте Telegram - должно прийти уведомление
3. Проверьте логи: `docker-compose logs -f backend`

**После этого все должно работать! 🚀**
