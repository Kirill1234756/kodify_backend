# 📝 Как вставить текст в nano через VNC

## ❌ Shift+Insert не работает? Используйте альтернативы:

---

## ✅ Способ 1: Правый клик мыши (САМЫЙ ПРОСТОЙ)

1. Скопируйте нужный текст (токен) где-нибудь еще
2. В nano - просто **правый клик мыши** в окне редактора
3. Выберите "Вставить" из контекстного меню

---

## ✅ Способ 2: Ввести вручную (надежный)

Токен короткий, его легко ввести вручную:

```
8226522901:AAHJSZ8zpZ-UCSX40TmKb_bLb12FwTgzGyA
```

**Главное:**

- После `TmKb` должно быть **подчеркивание `_`** (Shift + дефис на английской раскладке)
- НЕ дефис `-`

---

## ✅ Способ 3: Через команду чтения файла в nano

1. Сохраните токен в текстовый файл на вашем компьютере
2. Загрузите файл на VPS через SFTP/SCP
3. В nano: `Ctrl+R` (Read File)
4. Укажите путь к файлу с токеном
5. nano вставит содержимое файла

---

## ✅ Способ 4: Через echo и перенаправление (если nano уже открыт)

1. В **другом** терминале выполните:

   ```bash
   echo "8226522901:AAHJSZ8zpZ-UCSX40TmKb_bLb12FwTgzGyA" > /tmp/token.txt
   ```

2. В nano нажмите `Ctrl+R` и введите:

   ```
   /tmp/token.txt
   ```

3. nano вставит содержимое файла в текущую позицию курсора

---

## ✅ Способ 5: Редактировать через sed (без nano)

Если nano неудобен, можно использовать `sed` для прямой замены:

```bash
cd ~/projects/kodify/backend
sed -i 's/TELEGRAM_BOT_TOKEN=.*/TELEGRAM_BOT_TOKEN=8226522901:AAHJSZ8zpZ-UCSX40TmKb_bLb12FwTgzGyA/' .env
```

Это автоматически заменит строку с токеном на правильную.

---

## ✅ Способ 6: Использовать другой редактор (если установлен)

### Если установлен `vim` или `vi`:

```bash
nano .env
# Если не работает вставка, закройте (Ctrl+X)
vi .env
# В vim: нажмите 'i' для вставки, вставьте текст, нажмите Esc, введите ':wq' для сохранения
```

### Если установлен `nano` с поддержкой мыши:

Попробуйте включить поддержку мыши:

```bash
nano -m .env
```

---

## 🎯 РЕКОМЕНДУЕМЫЙ СПОСОБ для вашей ситуации:

Так как токен уже правильный (с подчеркиванием), просто:

1. **Сохраните файл:** `Ctrl+O`, `Enter`, `Ctrl+X`
2. **Обновите код и перезапустите:**
   ```bash
   cd ~/projects/kodify/backend
   git pull
   docker-compose restart backend
   ```

Если нужно исправить что-то еще, используйте **Способ 1 (правый клик)** или **Способ 5 (sed)**.

---

## ⚠️ Если ничего не работает:

Можно редактировать `.env` прямо через sed без nano:

```bash
# Посмотреть текущий токен
grep TELEGRAM_BOT_TOKEN ~/projects/kodify/backend/.env

# Заменить на правильный (одна команда)
sed -i 's/AAHJSZ8zpZ-UCSX40TmKb-bLb12FwTgzGyA/AAHJSZ8zpZ-UCSX40TmKb_bLb12FwTgzGyA/' ~/projects/kodify/backend/.env

# Проверить что заменилось
grep TELEGRAM_BOT_TOKEN ~/projects/kodify/backend/.env
```

Эта команда найдет и заменит дефис на подчеркивание автоматически!
