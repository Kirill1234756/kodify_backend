# ⚡ ИНСТРУКЦИЯ: Обновить .env на сервере

## Файл `env.production.full` УЖЕ в Git!

Если файл не видно на сервере, выполните:

```bash
cd ~/projects/kodify/backend
git pull
ls -la env.production.full
```

---

## Если файл НЕ найден - создать вручную:

Выполните эту команду на сервере (одна строка):

```bash
cd ~/projects/kodify/backend && cat > env.production.full << 'EOF'
# Production Environment Configuration
NODE_ENV=production
PORT=3000
FRONTEND_URL=https://kodifyweb.ru
FRONTEND_URLS=https://kodifyweb.ru,https://www.kodifyweb.ru
DB_HOST=postgres
DB_PORT=5432
DB_NAME=kodify_db
DB_USER=postgres
DB_PASSWORD=postgres
UPLOAD_DIR=./uploads
PUBLIC_URL=https://api.kodifyweb.ru
TELEGRAM_BOT_TOKEN=8226522901:AAHJSZ8zpZ-UCSX4OTmKb_bLbl2FwTgzGyA
TELEGRAM_CHAT_ID=-1003136896464
EOF
```

---

## Затем обновить .env:

```bash
cp env.production.full .env
docker-compose restart backend
```

---

## Проверка:

```bash
curl https://api.kodifyweb.ru/api/test/telegram
```

---

## Или одной командой (всё сразу):

```bash
cd ~/projects/kodify/backend && git pull && (test -f env.production.full && cp env.production.full .env || (cat > env.production.full << 'EOF'
NODE_ENV=production
PORT=3000
FRONTEND_URL=https://kodifyweb.ru
FRONTEND_URLS=https://kodifyweb.ru,https://www.kodifyweb.ru
DB_HOST=postgres
DB_PORT=5432
DB_NAME=kodify_db
DB_USER=postgres
DB_PASSWORD=postgres
UPLOAD_DIR=./uploads
PUBLIC_URL=https://api.kodifyweb.ru
TELEGRAM_BOT_TOKEN=8226522901:AAHJSZ8zpZ-UCSX4OTmKb_bLbl2FwTgzGyA
TELEGRAM_CHAT_ID=-1003136896464
EOF
cp env.production.full .env)) && docker-compose restart backend
```
